apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: postgres
  labels:
    app: postgres
data:
  POSTGRES_DB: immich
  POSTGRES_USER: postgres
  PGDATA: /var/lib/postgresql/data/pgdata
